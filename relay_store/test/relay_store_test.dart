import 'package:test/test.dart';

import 'package:relay_store/relay_store.dart';

void main() {
  test('adds one to input values', () {
    expect(1, 1);
    // expect(() => calculator.addOne(null), throwsNoSuchMethodError);
  });
}
